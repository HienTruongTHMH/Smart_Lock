{
  "name": "smart-lock-api",
  "version": "1.0.0",
  "description": "Smart Lock API for IoT project",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "vercel dev",
    "build": "echo 'No build step required for API'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "pg": "^8.11.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
