{
  "name": "smart-lock-api",
  "version": "1.0.0",
  "description": "Smart Lock API for IoT project",
  "main": "index.js",
  "scripts": {
    "start": "vercel dev",
    "dev": "vercel dev",
    "build": "echo 'No build needed for serverless functions'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
